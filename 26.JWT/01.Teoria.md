# Tokens: JWT 
## ¿Qué son los tokens?
Los tokens son como tu pase VIP digital: una cadena única que identifica al usuario sin tener que pedirle el usuario y contraseña cada dos minutos.
Sirven para autenticar y autorizar acciones en una app sin almacenar información sensible en el frontend.

### Beneficios de usar tokens

- 🔒 **Seguridad**: Evitás mandar credenciales en cada request.
- ⚡ **Rendimiento**: Autenticación más ligera y rápida.
- 🌐 **Escalabilidad**: Perfectos para APIs distribuidas y microservicios.
- ⏱️ **Experiencia fluida**: Sesiones más cómodas sin recargas innecesarias.

## JWT (JSON Web Token)

JWT es un estándar para crear tokens compactos, firmados y seguros para transmitir información entre cliente y servidor.
Se envía en formato JSON y se firma con una clave secreta para que nadie pueda modificarlo sin ser detectado.

- Un JWT es ideal para:
    - Autenticación
    -Autorización
    - Intercambio seguro de datos entre servicios

## Estructura del JWT

Un JWT está compuesto por 3 partes separadas por puntos (.):

1. **Header**
Define el algoritmo de firmado y el tipo de token.
Ejemplo:

```json
{
  "alg": "HS256",
  "typ": "JWT"
}
``` 

2. **Payload**
Contiene la información (claims) del usuario o la sesión.
Ejemplo:
```json
{
  "id": "123",
  "role": "admin",
  "exp": 1712345678
}
``` 

3. **Signature**
Es la firma generada a partir del header, el payload y la clave secreta.
Garantiza que nadie pueda alterar el token sin romper su validez.


## ¿Por qué un JWT es auto-contenido?

Un JWT es auto-contenido porque dentro del mismo token viaja toda la información necesaria para validar la identidad del usuario.
No necesita consultar una base de datos en cada request para saber quién sos: el servidor puede verificar el token solo con la clave secreta.

- ¿Qué incluye para ser auto-contenido?
    - 🧾 Datos del usuario (claims)
    - ⏳ Tiempos de expiración
    - 🎫 Roles o permisos
    - 🔐 Firma digital para validar que no fue modificado

- Ventajas de que sea auto-contenido
    - ⚡ Menos carga en el servidor
    - 🔄 Autenticación rápida
    - 🧩 Perfecto para sistemas distribuidos o APIs sin estado (stateless)
    - 🚫 No requiere una sesión almacenada en el servidor